<utils:WindowBase x:Class="FaPA.GUI.Feautures.User.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:m="clr-namespace:FaPA.GUI.Controls.MyTabControl"
                  xmlns:utils="clr-namespace:FaPA.Infrastructure.Utils"
                  SizeToContent="WidthAndHeight"
        TextOptions.TextFormattingMode="Display"
        Title="Utenti" >

    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*">
            </ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <TextBlock Text="Utenti" Style="{StaticResource Title}"
				   Grid.Row="0" TextAlignment="Center" VerticalAlignment="Top"/>

        <DockPanel Height="Auto" Grid.Row="1" Width="Auto">

            <Border Style="{StaticResource MainBorderStyle}" >
                <m:MyTabControl />
            </Border>

        </DockPanel>


    </Grid>
</utils:WindowBase>

